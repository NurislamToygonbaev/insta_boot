spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/insta_boot
        username: postgres
        password: rootroot
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true

#    flyway:
#        baseline-on-migrate: true
#        enabled: true
#        locations: "classpath:migration"